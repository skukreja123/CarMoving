# Define the Agent class
class Agent:
    def __init__(self, agent_id, position, strategy):
        self.agent_id = agent_id
        self.position = position
        self.strategy = strategy
        self.points = 0  # Initialize points for each agent
        
    
